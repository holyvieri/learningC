#include <stdio.h>
#include <stdlib.h>

int main(){

	int x; 
	int y;
	printf("tell me only two numbers pls: \n");
	scanf("%d", &x);
    scanf("%d", &y);
		
		int R = x * y ;

	printf("now, we will multiply them...and here it is: %d x %d = %d\n", x, y, R);
	
	}
